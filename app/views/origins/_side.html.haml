.side
  .side__title
    Money Save
  .side__user
    = current_user.name
    .side__user__icon
      = link_to edit_user_registration_path do
        = icon('fas', 'user-edit')
      = link_to destroy_user_session_path, method: :delete do
        = icon('fas', 'sign-out-alt')
  .side__info
    .side__info__register
      = link_to '収支登録', new_event_path
    .side__info__edit
      -# = link_to "収入一覧", income_path
    